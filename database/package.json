{
  "name": "@taskosaur/database",
  "version": "0.1.0",
  "description": "Taskosaur Database - Prisma Schema and Migrations",
  "private": true,
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:studio": "prisma studio",
    "prisma:migrate:dev": "prisma migrate dev",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "prisma:reset": "prisma migrate reset",
    "prisma": "prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0"
  },
  "devDependencies": {
    "prisma": "^6.19.0"
  }
}
